# CRITICAL: These are example values. You MUST change them before running the application.
# The application will not start without valid credentials configured in your .env file.
# ------------------------------------------------------------------------------------

# Example environment variables for docker-compose (Postgres)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=richk
DB_PASSWORD=changeme
POSTGRES_DB=richkware

# Encryption Key - MUST be changed in production
ENCRYPTION_KEY=ChangeThisSecureKey123!

# Spring Profile
SPRING_PROFILES_ACTIVE=docker
